{
  "name": "itsypoker",
  "version": "1.0.0",
  "description": "Planning poker application with WebRTC",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "client:dev": "cd client && npm run dev",
    "client:build": "cd client && npm run build",
    "server:start": "cd server && npm run start"
  },
  "type": "module",
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@vitest/ui": "^3.2.4",
    "happy-dom": "^18.0.1",
    "vitest": "^3.2.4"
  }
}
